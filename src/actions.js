export default {
	/* [ADD_PRODUCT_TO_CART2]({ commit, getters }, payload) {
		let cartItem = getters.getCartItem(payload.product);
		
		if (cartItem == null) {
			commit(ADD_PRODUCT_TO_CART2, payload);
			
		} else {
			commit(INCREASE_PRODUCT_QUANTITY, payload);
			
		}
	} */
}